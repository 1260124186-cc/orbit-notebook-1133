# Orbit Notebook

## 项目目标
Orbit Notebook 是一个面向小型天文台的本地观测资料服务，用于把一次观测计划、现场采集的天体描述和审核发布过程整理成可追踪的科学资料。系统使用本地 JSON 文件保存数据，不依赖外部服务。

## 用户角色
- 观测员：创建观测条目并补充现场描述。
- 审核员：检查条目的完整性并给出审核结论。
- 发布员：将已审核条目变成可检索的公开资料。

## 核心实体
- Observation：一次天体观测条目，包含目标、观测时间、仪器和描述。
- Instrument：参与观测的仪器信息。
- Review：审核员针对观测条目的检查结果。
- Bulletin：已发布的观测资料摘要。

## 业务流程
1. 创建观测条目：观测员提供目标和仪器，系统校验字段并保存草稿；无效时间或缺少目标时拒绝。
2. 补充观测描述：观测员为草稿追加描述和标签，系统合并内容并保留更新时间；不存在或已发布的条目不可追加。
3. 审核观测条目：审核员提交通过或退回意见，系统只允许完整草稿进入审核；不通过的条目回到草稿状态。
4. 发布观测资料：发布员选择已通过审核的条目，系统生成摘要并写入资料索引；重复发布和未审核条目均拒绝。
5. 查询观测资料：用户按目标、标签和状态筛选条目，系统返回稳定排序的摘要列表。

## 状态与规则
观测条目状态按 draft、reviewed、released 流转。每条记录必须有非空目标、仪器、观测时间和描述；审核通过后不能再修改；发布操作必须幂等地拒绝重复发布。所有写入通过原子临时文件替换完成，避免中断留下半成品。

## 接口与验证
项目形态为 CLI，入口为 `go run ./cmd/orbit-notebook`。命令包括 `capture`、`annotate`、`review`、`release`、`list` 和 `show`。运行清单会使用独立临时目录验证创建、补充、审核发布和查询流程，并检查错误输入有明确输出。
